function longest_name(arr) {
  for (let i = 0, j = 1; i, j < arr.length; i++, j++)
  arr.push(prompt("Enter a number");
    if (arr[i].length < arr[j].length) console.log(arr[j]);
}
longest_name(["John", "Tom", "Benjamin"]);
